# Campus contract — generated tables (kernel 5.2.9)

GENERATED by `tools/build_contract.py` from `contracts/campus.json`. Do not edit; edit the owning source named in each section.

## Hosts (owner: PLATFORM.md facts; literals parity-checked)

| Client | Manifest dir | Entry file (write) | Seat default |
|---|---|---|---|
| `claude-cowork` | `.claude-plugin` | `CLAUDE.md` | primary-eligible |
| `claude-code` | `.claude-plugin` | `CLAUDE.md` | satellite |
| `codex` | `.codex-plugin` | `AGENTS.md` | satellite |

Manifest probe order: `.claude-plugin/plugin.json` → `.codex-plugin/plugin.json` → `plugin.json` → `.agents/plugins/marketplace.json`. Entry files accepted on read: `CLAUDE.md`, `AGENTS.md`, `AGENTS.override.md`, `CODEX.md`.

## Shared-context files (owner: templates/context-files.md)

| Concept | Canonical | Founding writes | Legacy aliases |
|---|---|---|---|
| Owner/business basics | `identity.md` | `business.md` | `business.md` |
| Audience / ICP | `icp.md` | `audience.md` | `audience.md`, `icp-summary.md` |
| Brand | `brand.md` | `brand.md` | `BRAND.md`, `brand-style.md` |
| Voice / tone | `voice.md` | `voice.md` | `voice-profile.md`, `brand.md` |
| Goals / OKRs | `goals.md` | `goals.md` | — |
| Offers | `offers-summary.md` | `offers.md` | `offers.md` |
| Tool/connection map | `connections.md` | `tools.md` | `tools.md`, `campus-connections.md`, `SYSTEMS.md` |

## Registry enums (owner: templates/registration-block-spec.md)

| Field | Values |
|---|---|
| `type` | `plugin` · `authored-skill` · `kernel` · `department` · `systems` |
| `status` | `active` · `legacy` · `disabled` · `retired` |
| `department` | `marketing` · `sales` · `education` · `community` · `lead-generation` · `operations` · `finance` · `people` · `dean-office` · `kernel` · `core` · `cross` |
| legacy read alias | `dean` → `dean-office` |

## Seat identity (owner: tools/seat_contract_checks.py + ledger-paths.md)

Roles: primary, satellite. States: valid, missing, invalid, unresolved. Seat id: `[A-Za-z0-9][A-Za-z0-9_.-]*`.

| Role | Ledger | Lane |
|---|---|---|
| primary | `dean/.activity.jsonl` | (whole campus) |
| satellite | `dean/.activity.{seat}.jsonl` | `active-work/lanes/{seat}/` |
| unknown seat | `dean/.activity.unknown.jsonl` | `active-work/lanes/{seat}/` |

Primary-owned paths: `dean/memory.md`, `dean/TASKS.md`, `dean/tasks/`, `wiki/`, `shared/`, `.campus-os/registry.json`.

## run_id (owner: tools/mint_run_id.py)

Write: `{slug}-{YYYYMMDD-HHMMSSfff}[-xxxx]`. Run dir: `outputs/{YYYY-MM}/runs/{team}/{slug}/{run_id}/`.

| Shape | Regex |
|---|---|
| canonical | `^(?P<slug>.+)-(?P<date>\d{8})-(?P<time>\d{6})(?P<millis>\d{3})(?:-(?P<suffix>[0-9a-f]{4}))?$` |
| legacy_dash | `^(?P<slug>.+)-(?P<date>\d{8})-(?P<time>\d{6})$` |
| legacy_compact | `^(?:(?P<slug>.+)-)?(?P<date>\d{8})T(?P<time>\d{6})Z$` |
| legacy_iso_minute | `^(?:(?P<slug>.+)-)?(?P<date>\d{4}-\d{2}-\d{2})T(?P<time>\d{4})Z$` |
| legacy_date_slug_time | `^(?P<date>\d{8})-(?P<slug>.+)-(?P<time>\d{4})$` |

## Results envelope (owner: schemas/results.schema.json)

Write `schema_version` `1.1`; read `1.0`, (missing), `1.1-completion-only` as legacy. Schema sha256 `e4beae70ed38…`.

## Capability record (R1)

Required: `id`, `kind`, `permission_ceiling`. Kinds: `kernel-skill`, `team`, `playbook`, `hire`.
