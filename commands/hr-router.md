---
description: "Set up Lite mode - run routine steps on a lighter engine"
---

Optional. Set up a routing policy so routine steps (summarize, sort, short drafts) can run on a lighter engine while voice, publishing, money, and judgment stay on the full model. Dean writes the policy; you approve and enable it. The publish pin can never be removed.

$ARGUMENTS
